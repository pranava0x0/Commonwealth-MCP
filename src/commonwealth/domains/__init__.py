"""Domain packages (../../../design/architecture.md decision 0001): registry (discovery) and geo for V1;
civic arrives at milestone 1b. Domain modules import core, adapters, and
runtime — never each other, and never mcp."""
